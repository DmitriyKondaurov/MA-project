import { FillAmountDirective } from './fill-amount.directive';

describe('FillAmountDirective', () => {
  it('should create an instance', () => {
    const directive = new FillAmountDirective();
    expect(directive).toBeTruthy();
  });
});

import { CostsFilterPipe } from './costs-filter.pipe';

describe('CostsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CostsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
